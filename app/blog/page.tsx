import Link from 'next/link'
import {articles} from '@/lib/editorial-blog'
export const metadata={title:'Guides and Articles',description:'Practical guidance for choosing a provider and preparing for your next conversation.'}
export default function Blog(){const posts=articles();return <section className="mx-auto max-w-4xl px-6 py-16"><h1 className="font-serif text-4xl font-semibold mb-4">Guides and Articles</h1><p className="mb-10">Practical information to help you ask informed questions and find the right support.</p><div className="grid gap-6">{posts.map(p=><article key={p.slug} className="border rounded-2xl p-7"><time className="text-sm opacity-70">{p.date}</time><h2 className="text-2xl font-serif my-3"><Link href={'/blog/'+p.slug}>{p.title}</Link></h2><p>{p.excerpt}</p><Link className="underline inline-block mt-4" href={'/blog/'+p.slug}>Read article →</Link></article>)}</div>{!posts.length&&<p>New guides are being prepared. <Link href="/listings" className="underline">Browse the directory</Link>.</p>}</section>}
